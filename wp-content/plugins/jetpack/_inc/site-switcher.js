import apiFetch from'@wordpress/api-fetch';import{useCommandLoader}from'@wordpress/commands';import{useMemo,useState,useEffect}from'@wordpress/element';import{sprintf,__}from'@wordpress/i18n';import{siteLogo}from'@wordpress/icons';const userId=window.jetpackSiteSwitcherConfig?.userId||0;const CACHE_KEY=`jetpack_site_switcher_sites_${ userId }`;const CACHE_DURATION=3600000;function getCachedSites(){try{const cached=localStorage.getItem(CACHE_KEY);if(!cached){return null;}
const{sites,timestamp}=JSON.parse(cached);if(Date.now()-timestamp<CACHE_DURATION){return sites;}
localStorage.removeItem(CACHE_KEY);return null;}catch{return null;}}
function setCachedSites(sites){try{localStorage.setItem(CACHE_KEY,JSON.stringify({sites,timestamp:Date.now(),}));}catch{}}
async function fetchSitesFromWordPressCom(){const cachedSites=getCachedSites();if(cachedSites){return cachedSites;}
const apiPath=window.jetpackSiteSwitcherConfig?.apiPath;try{const data=await apiFetch({path:apiPath,method:'GET',global:true,});const sites=data.sites||[];setCachedSites(sites);return sites;}catch{return[];}}
function getHostnameFromURL(urlString){if(!urlString){return'';}
try{return new URL(urlString).hostname;}catch{return'';}}
function untrailingslashit(url){return url?url.replace(/\/+$/,''):url;}
function escapeRegex(str){return str.replace(/[.*+?^${}()|[\]\\]/g,'\\$&');}
function useSiteSwitcherCommandLoader({search}){const[sites,setSites]=useState([]);const[isLoading,setIsLoading]=useState(true);useEffect(()=>{fetchSitesFromWordPressCom().then(fetchedSites=>{setSites(fetchedSites);setIsLoading(false);}).catch(()=>{setIsLoading(false);});},[]);const commands=useMemo(()=>{if(!sites||sites.length===0){return[];}
const searchLower=search?search.toLowerCase():'';const genericKeywords=[__('site','jetpack').toLowerCase(),__('switch','jetpack').toLowerCase(),__('switch site','jetpack').toLowerCase(),];let cleanedSearch=searchLower;genericKeywords.forEach(keyword=>{cleanedSearch=cleanedSearch.replace(new RegExp(`\\b${ escapeRegex( keyword ) }\\b`,'g'),' ');});cleanedSearch=cleanedSearch.trim().replace(/\s+/g,' ');const isGenericKeywordPrefix=cleanedSearch&&genericKeywords.some(keyword=>keyword.startsWith(cleanedSearch));const filteredSites=!cleanedSearch||isGenericKeywordPrefix?sites:sites.filter(site=>{const domain=getHostnameFromURL(site.URL);return((site.name&&site.name.toLowerCase().includes(cleanedSearch))||domain.toLowerCase().includes(cleanedSearch));});const validSites=filteredSites.filter(site=>{return site.URL&&getHostnameFromURL(site.URL)!=='';});const currentURL=untrailingslashit(window.location.href.toLowerCase());const otherSites=validSites.filter(site=>{const siteURL=untrailingslashit(site.URL.toLowerCase());return!currentURL.startsWith(siteURL);});return otherSites.map(site=>{const domain=getHostnameFromURL(site.URL);const iconElement=site.icon?.img?<img src={site.icon.img}alt=""/>:siteLogo;const label=site.name?sprintf(__('Switch to %1$s (%2$s)','jetpack'),site.name,domain):sprintf(__('Switch to %s','jetpack'),domain);return{name:`jetpack/switch-to-site-${ domain }`,label,icon:iconElement,callback:({close})=>{try{window.location.href=new URL('/wp-admin',site.URL).href;}catch{}
close();},keywords:[site.name,domain,__('site','jetpack'),__('switch site','jetpack'),].filter(Boolean),};});},[sites,search]);return{commands,isLoading,};}
function JetpackSiteSwitcher(){useCommandLoader({name:'jetpack/site-switcher',hook:useSiteSwitcherCommandLoader,});return null;}
if(typeof window!=='undefined'&&window.wp&&window.wp.element&&window.wp.commands){const{createRoot,createElement}=window.wp.element;const container=document.createElement('div');container.id='jetpack-site-switcher';container.style.display='none';if(document.readyState==='loading'){document.addEventListener('DOMContentLoaded',()=>{document.body.appendChild(container);createRoot(container).render(createElement(JetpackSiteSwitcher));});}else{document.body.appendChild(container);createRoot(container).render(createElement(JetpackSiteSwitcher));}}