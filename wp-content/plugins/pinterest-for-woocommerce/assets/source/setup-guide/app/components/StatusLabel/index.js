import classnames from'classnames';import{Icon}from'@wordpress/components';import{Spinner}from'@woocommerce/components';import{LABEL_STATUS as STATUS}from'../../constants';import'./style.scss';const compClassName='pfw-status-label';const statusStyleName={[STATUS.PENDING]:`${ compClassName }--status-pending`,[STATUS.SUCCESS]:`${ compClassName }--status-success`,};function getStatusPrefix(status){switch(status){case STATUS.PENDING:return<Spinner />;case STATUS.SUCCESS:return<Icon icon="yes-alt"/>;}}
export default function StatusLabel({status,text}){const className=classnames(compClassName,statusStyleName[status]);return(<div className={className}>{getStatusPrefix(status)}{text}</div>);}